# Application: apollo11-prod
#
# Production — manual sync ONLY. No automated sync, no selfHeal.
# A human reviews the diff in the ArgoCD UI (or via `argocd app diff`)
# and explicitly clicks "Sync" or runs `argocd app sync apollo11-prod`.
#
# Values:    values-prod.yaml (3 replicas, PDBs, GHCR pull)
# Image tag: pinned to v1.0.0 by default. Update this file (or pass
#            --helm-set image.tag=... in the CLI) to roll forward.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apollo11-prod
  namespace: argocd
  annotations:
    argocd.argoproj.io/compare-options: ServerSideDiff=true
  labels:
    app.kubernetes.io/part-of: apollo-airlines
    app.kubernetes.io/component: gitops
    apollo-airlines/environment: prod
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: apollo-airlines

  source:
    repoURL: https://github.com/darshan-raul/Apollo11.git
    # Prod is manual-sync. Pin this to the release tag once v1.0.0 exists;
    # using a nonexistent revision makes the Application permanently invalid.
    targetRevision: main
    path: stages/stage7/helm/apollo11
    helm:
      valueFiles:
        - values-prod.yaml
      parameters:
        - name: image.repository
          value: ghcr.io/darshan-raul/apollo11
        - name: image.tag
          value: v1.0.0
        - name: namespaces.apps
          value: apollo-airlines-prod-apps
        - name: namespaces.ui
          value: apollo-airlines-prod-ui
        - name: pdb.enabled
          value: "true"
        # In prod, the chart's bundled Envoy + MetalLB installs are
        # disabled because we provision the access stack at cluster
        # bootstrap time (see terraform/eks or kind setup scripts).
        - name: gateway.envoy.bundleInstall
          value: "false"
        - name: gateway.createClass
          value: "false"
        - name: autoscaling.metricsServer.bundleInstall
          value: "false"
        - name: vpa.bundleInstall
          value: "false"
        - name: priorityClasses.enabled
          value: "false"
        - name: metallb.enabled
          value: "false"
        - name: observability.enabled
          value: "false"

  destination:
    server: https://kubernetes.default.svc
    namespace: apollo-airlines-prod-apps

  # NO automated sync, NO selfHeal. A human gates every change.
  # This is the prod contract: "kubectl edit" still works for incident
  # response, but ArgoCD won't silently revert it. To revert, sync
  # against the previous revision via `argocd app rollback apollo11-prod`.
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m

  ignoreDifferences:
    - group: admissionregistration.k8s.io
      kind: ValidatingWebhookConfiguration
      jsonPointers:
        - /webhooks/0/clientConfig/service/namespace
    - group: 'apps'
      kind: Deployment
      # Allow SREs to bump replicas by hand for incident response
      jsonPointers:
        - /spec/replicas
    - group: '*'
      kind: 'Pod'
      jqPathExpressions:
        - '.spec.containers[].resources'

  # Keep a longer history in prod so we can roll back further
  revisionHistoryLimit: 50
