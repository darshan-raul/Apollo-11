# Launchpad-only development credentials. Copy this file to .env and change the
# values before starting Compose. Use URL-safe characters in POSTGRES_PASSWORD
# because it is embedded in DATABASE_URL values.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=apollo_local_postgres
JWT_SECRET=replace-with-a-long-random-development-secret
