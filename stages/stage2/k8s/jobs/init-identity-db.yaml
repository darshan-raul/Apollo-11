apiVersion: batch/v1
kind: Job
metadata:
  name: init-identity-db
  namespace: apollo-airlines-apps
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: init-identity-db
    spec:
      serviceAccountName: init-identity-db
      containers:
        - name: init
          image: postgres:15-alpine
          command:
            - sh
            - -c
            - |
              set -eu
              attempt=0
              until pg_isready -h identity-db -U postgres; do
                attempt=$((attempt + 1))
                if [ "${attempt}" -ge 30 ]; then
                  echo "identity-db did not become ready" >&2
                  exit 1
                fi
                echo "Waiting for identity-db... (${attempt}/30)"
                sleep 2
              done
              psql -v ON_ERROR_STOP=1 -h identity-db -U postgres -d identity -f /init/init.sql
              echo "identity DB init done"
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: apollo-airlines-secrets
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: init-script
              mountPath: /init
      volumes:
        - name: init-script
          configMap:
            name: identity-init-script
      restartPolicy: Never
