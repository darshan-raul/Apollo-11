# Application: apollo11-staging
#
# Mirror of dev, but with values-staging.yaml: 2 replicas per app, image
# tag :latest, no PDBs (staging is for integration testing, not
# exercising eviction policies).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: apollo11-staging
  namespace: argocd
  annotations:
    argocd.argoproj.io/compare-options: ServerSideDiff=true
  labels:
    app.kubernetes.io/part-of: apollo-airlines
    app.kubernetes.io/component: gitops
    apollo-airlines/environment: staging
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: apollo-airlines

  source:
    repoURL: https://github.com/darshan-raul/Apollo11.git
    targetRevision: HEAD
    path: stages/stage6/helm/apollo11
    helm:
      valueFiles:
        - values-staging.yaml
      parameters:
        - name: image.repository
          value: ghcr.io/darshan-raul/apollo11
        - name: image.tag
          value: latest
        - name: namespaces.apps
          value: apollo-airlines-staging-apps
        - name: namespaces.ui
          value: apollo-airlines-staging-ui
        - name: pdb.enabled
          value: "false"
        - name: gateway.createClass
          value: "false"
        - name: gateway.envoy.bundleInstall
          value: "false"
        - name: metallb.enabled
          value: "false"
        - name: observability.enabled
          value: "false"

  destination:
    server: https://kubernetes.default.svc
    namespace: apollo-airlines-staging-apps

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ServerSideApply=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: admissionregistration.k8s.io
      kind: ValidatingWebhookConfiguration
      jsonPointers:
        - /webhooks/0/clientConfig/service/namespace
    - group: '*'
      kind: 'Pod'
      jqPathExpressions:
        - '.spec.containers[].resources'

  revisionHistoryLimit: 10
